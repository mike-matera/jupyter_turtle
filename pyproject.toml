[project]
name = "jupyter_turtle"
version = "0.0.1"
dependencies = [
    'ipycanvas',
    'importlib-metadata; python_version<"3.8"',
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
